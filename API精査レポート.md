# 天気予報 API 精査レポート

- 作成日: 2026-09-21
- 要件: 2週間(14日)予報 / 気温 / 湿度 / 愛知県豊橋市・東京の切替 / スマートフォン向け

---

## 1. 結論

**Open-Meteo Forecast API を主軸に採用することを推奨する。**

決定打は「**14日以上・湿度あり・APIキー不要**」を同時に満たすのが Open-Meteo だけだった点。
APIキーが不要ということは、**サーバを一切立てずに、スマホのブラウザから直接 API を叩ける**。
キーを隠すためのバックエンドも、その運用費も、キー流出のリスクも発生しない。
これはスマホ向け個人アプリでは非常に大きい。

補助案として、直近7日だけ気象庁(JMA)を併用する構成も成立する（後述 §5）。採否は要判断。

---

## 2. 候補比較

| API | 予報日数(無料) | 湿度 | APIキー | CORS | 無料枠 | 判定 |
|---|---|---|---|---|---|---|
| **Open-Meteo** | **16日** | ○ (時別) | **不要** | ○ `*` | 10,000回/日 | **◎ 採用** |
| 気象庁 (JMA) | 7日 | **×** | 不要 | ○ `*` | 明示なし | △ 併用候補 |
| OpenWeatherMap | 5日(3時間毎) | ○ | 必要 | ○ | 1,000回/日 | **× 16日予報は有料** |
| Visual Crossing | 15日 | ○ | 必要 | ○ | 1,000レコード/日 | △ 次点 |
| WeatherAPI.com | 14日 | ○ | 必要 | ○ | 情報が錯綜 | △ 次点 |

### 各候補の落選理由

- **OpenWeatherMap** — 最有名だが、**16日予報(forecast16)は無料プランに含まれない**。公式の料金表で
  無料欄が鍵アイコン、Startup以上でチェックになっていることを確認済み。無料で使えるのは5日/3時間予報まで。
  今回の「2週間」要件と正面から衝突するため落選。
- **気象庁 (JMA)** — 日本の公式一次情報で精度の信頼性は最上位だが、**週間予報が7日まで**、
  かつ**湿度の項目が一切ない**（実測で確認）。単独では要件を満たせない。
- **Visual Crossing / WeatherAPI.com** — 日数は足りるが APIキーが必要。
  スマホアプリから直接叩くとキーがソースに露出するため、キーを隠すサーバが要る。
  「無料・サーバなし」で済む Open-Meteo と比べて構成が重くなるので次点扱い。

---

## 3. 実測ログ（この精査で実際に叩いて確認した事実）

推測ではなく、以下は 2026-09-21 に実行した結果である。

### 3-1. Open-Meteo / 豊橋市 (34.7692, 137.3915)

```
HTTP 200 / 応答 2.37秒 / 9,869 バイト
返却日数: 16日 (2026-09-21 〜 2026-10-06)
```

**16日分すべて、全項目で欠損(null)ゼロ。** 後半の日だけ降水確率が欠ける、といった穴がないことを確認した。

| 項目 | null件数 |
|---|---|
| weather_code (天気コード) | 0 |
| temperature_2m_max / min (最高/最低気温) | 0 |
| apparent_temperature_max / min (体感温度) | 0 |
| precipitation_probability_max (降水確率) | 0 |
| wind_speed_10m_max (最大風速) | 0 |

時別湿度 `relative_humidity_2m` は 384件 = 16日 × 24時間 で完全に揃っていた。

取得できたデータの一部（豊橋市）:

| 日付 | 気温 | 体感温度 | 降水確率 | 日平均湿度 |
|---|---|---|---|---|
| 2026-09-21 | 22.8〜30.2℃ | 26.7〜33.3℃ | 99% | 79% |
| 2026-09-26 | 19.4〜21.7℃ | 21.5〜25.0℃ | 71% | 89% |
| 2026-10-03 | 15.5〜23.2℃ | 14.0〜22.2℃ | 25% | 61% |
| 2026-10-06 | 20.6〜26.0℃ | 21.2〜28.0℃ | 35% | 75% |

### 3-2. CORS（ブラウザから直接叩けるか）

```
access-control-allow-origin: *
access-control-allow-methods: GET, POST, OPTIONS
```

**確認済み。** スマホのブラウザから fetch() で直接叩ける。中継サーバは不要。

### 3-3. 気象庁 (JMA) 実測

```
愛知県 (230000): HTTP 200 / 3,629 バイト
東京都 (130000): HTTP 200 / 6,326 バイト
CORS: Access-Control-Allow-Origin: *
```

構造を解析した結果:

- 週間予報ブロックは **7日分** (2026-09-22〜2026-09-28)
- 含まれる項目: `tempsMax` `tempsMin` (上下限付き) / `pops` (降水確率) / `weatherCodes` / **`reliabilities` (信頼度 A・B・C)**
- **湿度の項目は存在しない**
- 愛知県は「西部 / 東部」の2区分。**豊橋市は「東部」**に属する

`reliabilities`（週間予報の信頼度ランク）は Open-Meteo にはない、気象庁ならではの有用な情報。

---

## 4. 採用する API 仕様（Open-Meteo）

### エンドポイント

```
https://api.open-meteo.com/v1/forecast
```

### パラメータ

| パラメータ | 値 | 用途 |
|---|---|---|
| `latitude` / `longitude` | 下表の座標 | 地点指定 |
| `daily` | `weather_code,temperature_2m_max,temperature_2m_min,apparent_temperature_max,apparent_temperature_min,precipitation_probability_max,wind_speed_10m_max,sunrise,sunset,uv_index_max` | 日別データ |
| `hourly` | `relative_humidity_2m,temperature_2m,apparent_temperature,precipitation_probability` | 時別データ（湿度はここから） |
| `timezone` | `Asia/Tokyo` | JST で日界を切る |
| `forecast_days` | `16` | **既定は7日なので必ず明示する** |

### 地点座標

| 都市 | 緯度 | 経度 | 備考 |
|---|---|---|---|
| 愛知県豊橋市 | 34.7692 | 137.3915 | 豊橋市役所付近。API返却標高 12m |
| 東京 | 35.691667 | 139.75 | **気象庁の東京観測点（千代田区北の丸公園・確定）**。API返却標高 32m / 気象庁公表 25m |

### 呼び出し例（そのまま動く）

```
https://api.open-meteo.com/v1/forecast?latitude=34.7692&longitude=137.3915&daily=weather_code,temperature_2m_max,temperature_2m_min,apparent_temperature_max,apparent_temperature_min,precipitation_probability_max,wind_speed_10m_max&hourly=relative_humidity_2m&timezone=Asia%2FTokyo&forecast_days=16
```

### 湿度の扱い（重要）

**Open-Meteo に「日別の湿度」は存在しない。** 時別 (`relative_humidity_2m`) のみ。
よって日別湿度はアプリ側で時別24件から集計して作る。

```js
// 例: i 日目の日平均湿度
const h = hourly.relative_humidity_2m.slice(i * 24, (i + 1) * 24);
const avg = Math.round(h.reduce((a, b) => a + b, 0) / h.length);
```

**採用: 日中平均（9〜18時）。** 夜間は湿度が上がって平均を押し上げるため、活動時間帯に絞る。
実測での差は小さくない（東京 2026-09-24: 24時間平均 84% に対し日中平均 71%）。

### 呼び出し回数の見積り

- 1都市あたり1回。2都市で2回。日別と時別は同一リクエストで同時に取れる
- 無料枠 10,000回/日 に対し、個人利用なら **実質無制限**
- ただし無駄打ちを避けるため、取得結果を localStorage に 30〜60分キャッシュする設計を推奨

---

## 5. 気象庁を併用する案（採否は要判断）

| | 単独案（Open-Meteo のみ） | 併用案（1〜7日は気象庁） |
|---|---|---|
| 実装量 | 少ない | 2つのAPIの日付突き合わせが必要で増える |
| 直近の精度 | 十分 | 日本の公式予報で、より信頼できる |
| 日本語の天気表現 | コードから自前で生成 | 「くもり時々晴れ」等がそのまま取れる |
| 信頼度ランク | なし | **A/B/C が取れる** |
| 湿度 | ○ | 気象庁側にはないので結局 Open-Meteo から補う |
| 不整合リスク | なし | **同じ日に2つの異なる予報が並ぶ**（見せ方の設計が要る） |

実装の手間と「表示が食い違って見える」リスクを考えると、**まずは Open-Meteo 単独で作り、
気象庁併用は後から足す**のが順当。§6-1 で確認したい。

---

## 6. ライセンスと注意点

### 6-1. Open-Meteo のライセンス

- データは **CC-BY 4.0**。**出典表記が必須**。アプリ内に「Weather data by Open-Meteo.com」を明記する
- **無料枠は非商用限定**。個人利用・非営利サイト・教育目的は無料の範囲内。
  広告掲載・課金・商用製品への組込みは有料プラン契約が必要
- 今回の用途（個人／教育）は無料枠の条件を満たすと判断できるが、**将来配布・公開する予定があるなら要確認**

### 6-2. 気象庁 API の注意

気象庁の JSON は **公式にはドキュメント化されていない**（防災情報サイトの内部用エンドポイント）。
予告なく仕様が変わりうる。併用する場合は、落ちても Open-Meteo だけで成立する作りにすること。

### 6-3. 未検証の情報

以下は検索結果からの二次情報で、**公式では未確認**。採用しないので深追いしていない。

- WeatherAPI.com の無料枠 — 「14日予報」「月100回」「月100万回」と情報源によって記述が食い違っており、信頼できない
- Visual Crossing の「無料枠でも商用可」

---

## 7. WMO 天気コード → 表示の対応表

Open-Meteo の `weather_code` は WMO 4677 準拠。画像素材の必要数に直結するため、ここで確定させる。

| コード | 意味 | 表示グループ |
|---|---|---|
| 0 | 快晴 | 晴れ |
| 1 | おおむね晴れ | 晴れ |
| 2 | 部分的に曇り | 晴れ時々曇り |
| 3 | 曇り | 曇り |
| 45, 48 | 霧・霧氷 | 霧 |
| 51, 53, 55 | 霧雨（弱・並・強） | 小雨 |
| 56, 57 | 着氷性の霧雨 | みぞれ |
| 61, 63, 65 | 雨（弱・並・強） | 雨 / 強い雨 |
| 66, 67 | 着氷性の雨 | みぞれ |
| 71, 73, 75 | 雪（弱・並・強） | 雪 |
| 77 | 霧雪 | 雪 |
| 80, 81, 82 | にわか雨（弱・並・強） | にわか雨 |
| 85, 86 | にわか雪 | 雪 |
| 95 | 雷雨 | 雷 |
| 96, 99 | 雹を伴う雷雨 | 雷 |

→ 表示グループに集約すると **10種類**（晴れ / 晴れ時々曇り / 曇り / 霧 / 小雨 / 雨 / 強い雨 / にわか雨 / みぞれ / 雪 / 雷）。
実測データには 1, 2, 3, 51, 53, 63, 80, 81 が出現した。

---

## 出典

- [Open-Meteo 公式ドキュメント](https://open-meteo.com/en/docs)
- [Open-Meteo 利用規約・無料枠](https://open-meteo.com/en/terms)
- [OpenWeather 料金比較表](https://openweathermap.org/full-price)
- [OpenWeatherMap 16日予報 API](https://openweathermap.org/api/forecast16)
- 気象庁 防災情報 JSON: `https://www.jma.go.jp/bosai/forecast/data/forecast/230000.json` (愛知県) / `130000.json` (東京都)
