# 「服装予報」確定仕様書

- 作成日: 2026-09-21
- グリルミー 第1段階（20問）＋ 第2段階（16問）の全36項目を反映
- 前身の `仕様メモ_第1段階.md` はこのファイルに統合済み

---

## 1. アプリ概要

| 項目 | 内容 |
|---|---|
| アプリ名 | **服装予報** |
| 形態 | **PWA**（ブラウザで開き、ホーム画面に追加で全画面動作） |
| 対象都市 | 愛知県豊橋市 / 東京（2都市固定） |
| 予報期間 | 16日（うち14日を主に表示） |
| 利用者 | 本人のみ |
| 主用途 | 朝の服選び／数日先の予定の準備 |
| 開発体制 | クロコ単独（画像のみ別AIへ発注） |

---

## 2. データ

### 2-1. API

| 項目 | 内容 |
|---|---|
| データ元 | **Open-Meteo Forecast API 単独**（気象庁は併用しない） |
| エンドポイント | `https://api.open-meteo.com/v1/forecast` |
| APIキー | **不要** |
| サーバ | **不要**（CORS が `*` のためブラウザから直接取得） |
| 更新 | **1時間キャッシュ**。オフライン時は取得時刻を添えて前回分を表示 |
| 出典表記 | **「Weather data by Open-Meteo.com」を画面内に必須表示**（CC-BY 4.0） |

**昨日との差にも別APIは不要。** `past_days=1` を付けると昨日＋16日＝17日分が
同じ1リクエストで返る（実装時に確認）。設計当初は archive API の併用を想定していたが、取りやめた。

### 2-2. 取得パラメータ

```
daily  = weather_code, temperature_2m_max, temperature_2m_min,
         apparent_temperature_max, precipitation_probability_max,
         wind_speed_10m_max, sunrise, sunset
hourly = relative_humidity_2m, temperature_2m, precipitation_probability
timezone = Asia/Tokyo
forecast_days = 16
past_days = 1          ← 昨日との差のため
```

### 2-3. 地点座標

| 都市 | 緯度 | 経度 | 根拠 |
|---|---|---|---|
| 愛知県豊橋市 | 34.7692 | 137.3915 | 豊橋市役所付近（API返却標高 12m） |
| 東京 | 35.691667 | 139.75 | **気象庁 東京観測点**（千代田区北の丸公園）。API返却標高 32m／気象庁公表 25m |

### 2-4. 湿度の集計（重要）

**Open-Meteo に「日別の湿度」は存在しない。** 時別のみ。日別値はアプリ側で作る。

- **採用: 日中平均（9〜18時の10件平均）**
- 理由: 夜間は湿度が上がって平均を押し上げる。実測（東京 2026-09-24）で
  **24時間平均 84% に対し日中平均 71%** と13ポイント差が出た

---

## 3. 服装判定ロジック

### 3-1. 判定基準

**体感温度の最高値（`apparent_temperature_max`）で判定する。**

### 3-2. 6段階（暑い → 寒い）

| 段階 | 服装 | 既定のしきい値 | 背景チップ色（ライト/ダーク共通） |
|---|---|---|---|
| 1 | 半袖・半ズボン | 28℃ 以上 | `#FFDDD2` 淡い赤橙 |
| 2 | 半袖・長ズボン | 24〜27℃ | `#FFEBCC` 淡い橙黄 |
| 3 | **シャツ姿くらい** | 20〜23℃ | `#E8F2D0` 淡い黄緑 |
| 4 | **長袖・長ズボン** | 16〜19℃ | `#D5EDE2` 淡い緑青 |
| 5 | ジャケットが必要 | 11〜15℃ | `#D5E5F5` 淡い水色 |
| 6 | コートが必要 | 10℃ 以下 | `#DDDCF2` 淡い青紫 |

> **注1: 3番目と4番目の順序。** 「シャツ姿くらい」は**シャツ1枚で過ごせる＝まだ暖かい**の意で、
> 長袖カットソーより**暑い側**。当初案は逆だった。
>
> **注2: チップ色はライト/ダークで反転させない。** アイコン画像は1枚しかないため、
> 背景を暗くすると濃い輪郭線が読めなくなる。淡い色のまま両モードで使う。

### 3-3. しきい値の調整機能

設定画面で **「暑がり ／ 標準 ／ 寒がり」** を選び、6つのしきい値を一括で **±2℃** ずらせる。

### 3-4. 雨・風の扱い

- 服装本体は**気温（体感温度）のみで決定**。雨風による段階の補正は**行わない**
- 降水確率 **50%以上**の日に、14日リストへ **傘アイコン**を添える
- **長靴は 70%以上**のときだけ追加する。50%で傘と長靴が常に同時に出ると、
  2つ並べても情報が増えないため実装時に分けた（しきい値は `POP_BOOTS` で変更可）
- 今日の欄には傘マークを出さない（時間別グラフで降水が見えるため）

---

## 4. 画面仕様

### 4-1. 構成

```
┌──────────────────────────┐
│  [ 豊橋 ] [ 東京 ]        ← 上部タブ（前回の選択を記憶）
├──────────────────────────┤
│  今日の欄（大きく）        │
│   ・天気アイコン           │
│   ・最高/最低気温          │
│   ・体感温度（併記）       │
│   ・昨日との差             │
│   ・湿度（日中平均）       │
│   ・服装イラスト（大）     │
│   ・時間別グラフ           │
├──────────────────────────┤
│  14日リスト（縦スクロール）│
│   1日 = 2行                │
│   ・8日目以降は薄く表示    │
├──────────────────────────┤
│  Weather data by Open-Meteo.com
└──────────────────────────┘
```

### 4-2. 14日リストの1行（**1日につき2行**を使う）

8要素を1行に詰めると375px幅で余白が尽きるため、2行に分ける。

```
上段: 9/21(月)   [天気アイコン]   22〜30℃
下段: 体感33℃   降水99%   [傘][服装アイコン(背景=段階色)]
```

- **8日目以降は文字色を薄くし、区切り線と「ここから先は参考値」の注記を入れる**
- **行をタップすると詳細が開く**（湿度・風速・日の出日の入など。幅を犠牲にせず14日分見られる）

### 4-3. 今日の欄

- 気温・天気・服装・湿度に加え、**時間別グラフ**を置く。
  **気温（℃）と降水確率（%）はスケールが違うため、1つのグラフに重ねない**（二軸チャートの禁止）。
  x軸を共有した**上下2つのグラフ**に分ける。気温＝折れ線（オレンジ）、降水確率＝棒（ブルー）。
  配色は dataviz スキルの検証済みパレットで、ライト・ダークとも全項目PASS を確認済み。
- **体感温度を併記**（例:「最高25℃（体感28℃）」）
- **昨日との差**を表示（例:「昨日より5℃低い」）

### 4-4. 見た目

| 項目 | 決定 |
|---|---|
| 絵柄 | フラットなベクター風 |
| 配色モード | **端末のライト/ダーク設定に追従** |
| 背景 | **CSSのグラデーション**（画像は使わない） |
| 天気アイコン | 自然な色（太陽=黄、雲=灰、雨=青） |
| 服装アイコン | 画像は無彩色寄り。**背景に段階色のチップ**を敷いて暑さ寒さを示す |
| 通知 | **なし** |

---

## 5. 画像素材（発注済み・計20枚）

| 分類 | 枚数 |
|---|---|
| 服装アイコン | 6 |
| 雨具（傘・長靴） | 2 |
| 天気アイコン | 11 |
| アプリアイコン | 1 |

詳細は `画像生成リスト_v2_発注書.md`。**段階色は画像に焼き込まず、アプリ側が背景に敷く。**

---

## 6. 検証で分かったこと（設計の根拠）

すべて実際にAPIを叩いて確かめた結果であり、推測ではない。

### 6-1. Open-Meteo は16日分すべてで欠損なし

豊橋の16日分で、天気コード・最高最低気温・体感温度・降水確率・風速のいずれも **null ゼロ**。
「後半の日だけ降水確率が抜ける」といった穴はなかった。

### 6-2. 体感温度と実気温の差は、季節で符号が反転する

過去350日（2025-10-01〜2026-09-15、豊橋）の月別集計。

| 月 | 体感−実気温 |
|---|---|
| 7月 | **+4.3℃** |
| 1月 | **−5.2℃** |
| 通年平均 | −0.2℃ |

**振れ幅は約9.5℃。** 通年平均がほぼ0に見えるのは夏と冬が打ち消し合っているため。
一律のしきい値補正では対応できない。

→ この結果、**画面に「最高25℃」と出ているのに半袖を勧める**ことが起きる（9月末の実データで発生）。
そのため**体感温度を併記して判定根拠を見せる**ことにした。

### 6-3. 最高気温で判定すると、朝は薄着になる

豊橋16日間で、朝（最低気温）と昼（最高気温）から導かれる服装段階を比較した。

```
段階が食い違う日: 15/16 日
2段階以上ずれる日:  9/16 日
日較差: 平均 6.3℃ / 最大 8.4℃
```

例: 10/4 は 14.7〜23.1℃ で、朝は「ジャケット」・昼は「シャツ姿」の2段階差。

→ **判定は日中（最高気温）のままとする方針をユーザーが選択。**
リストに最低気温を併記するため、朝の冷え込みは数字としては確認できる。

### 6-4. 傘マークのしきい値

豊橋16日間の降水確率で検算。

| しきい値 | 傘が付く日数 |
|---|---|
| 30%以上 | 13/16日（81%）— 多すぎて印として機能しない |
| 40%以上 | 7/16日 |
| **50%以上（採用）** | **6/16日** |
| 60%以上 | 5/16日 |

---

## 7. 実装時の注意

1. **出典表記を忘れない。** CC-BY 4.0 の条件。画面内に「Weather data by Open-Meteo.com」
2. **無料枠は非商用限定。** 個人利用の範囲を超えるなら有料プランの契約が必要
3. **`forecast_days=16` を明示する。** 既定は7日
4. **湿度は時別からの自作。** 日別変数は存在しない
5. **段階色チップはライト/ダークで反転させない**（§3-2 注2）
6. **傘・長靴アイコンは「高さ基準」で並べる。** 閉じた傘は縦横比が約1:3.6で、
   正方形の枠に `object-fit: contain` で入れると幅が4px程度に潰れて判別できなくなる
7. 都市の座標は定数として持つ。将来増やす場合に備え、配列の形にはしておく

---

## 8. 決定一覧（全36項目）

### 第1段階（浅く広く・20問）

| # | 項目 | 決定 |
|---|---|---|
| 1 | アプリ形態 | PWA |
| 2 | 利用者 | 本人のみ |
| 3 | 利用場面 | 朝の服選び／数日先の準備 |
| 4 | 開発体制 | クロコ単独 |
| 5 | 最初の画面 | 今日を大きく＋下に14日 |
| 6 | 2週間の並べ方 | 縦スクロールのリスト |
| 7 | 都市切替 | 上部タブ |
| 8 | 服装の出し方 | 今日は大きく、他は小さく |
| 9 | 判定基準 | 体感温度の最高値 |
| 10 | 6段階の順 | シャツ姿は長袖より暑い側 |
| 11 | 人物像 | 人物は描かず服だけ |
| 12 | 雨風の扱い | 傘マーク＋長靴（服装本体は補正しない） |
| 13 | 絵柄 | フラットなベクター風 |
| 14 | 配色 | 端末設定に追従 |
| 15 | 雨版の範囲 | 傘・長靴の2枚のみ追加 |
| 16 | 背景 | CSSグラデーション |
| 17 | 湿度の集計 | 日中平均（9〜18時） |
| 18 | 気象庁の併用 | 使わない |
| 19 | 東京の基準点 | 気象庁 東京観測点 |
| 20 | 都市の拡張 | 2都市固定 |

### 第2段階（深掘り・16問）

| # | 項目 | 決定 |
|---|---|---|
| 21 | 朝と昼の段階差 | 日中（最高気温）のまま |
| 22 | 体感温度の見せ方 | 併記する |
| 23 | 傘のしきい値 | 降水確率50%以上 |
| 24 | 気温の境界値 | 設定画面で±2℃調整可 |
| 25 | 今日の欄の追加項目 | 時間別グラフ |
| 26 | 14日リストの追加項目 | 降水確率・体感温度 |
| 27 | 8日目以降 | 薄く表示＋注記 |
| 28 | 差分表示 | 昨日との差 |
| 29 | 湿度の表示範囲 | 行タップで詳細に表示 |
| 30 | 傘の位置 | 14日リストのみ |
| 31 | 更新方式 | 1時間キャッシュ |
| 32 | 通知 | なし |
| 33 | 行の密度 | 1日を2行使う |
| 34 | 配色方針 | 服装の背景に段階色 |
| 35 | アプリ名 | 服装予報 |
| 36 | 初期都市 | 前回見ていた都市を記憶 |


---

## 9. 実装で分かったこと（仕様の修正点）

実装して初めて判明し、上記の本文に反映済みの3点。

| # | 当初の想定 | 実際 |
|---|---|---|
| 1 | 昨日との差は archive API を併用 | **`past_days=1` で1リクエストに収まる。** 別API不要 |
| 2 | 今日の欄に「気温と降水確率の時間別グラフ」を1つ | **二軸チャートは禁止。** x軸共有の2グラフに分割 |
| 3 | 傘・長靴を50%以上で併記 | 同条件で2つ出すと情報が増えないため、**長靴は70%以上**に分けた |

### 納品された画像の検品結果（2026-09-21）

20枚すべて合格。員数・ファイル名一致、全枚 1024×1024、純黒の塗り0px、
純白は wear_3 に102px（0.001%＝縁のアンチエイリアス）のみ。全枚に輪郭線があり、
白背景・黒背景の両方で消えないことを実機描画で確認した。

ただし**大きさが線寸法で11〜20%ばらつき**、wx_fog と wx_drizzle が中心から
最大84pxずれていたため、`build_assets.py` で正規化してから `app/img/` に配置している。
**納品原本（`assets/`）は書き換えていない。**
正規化後は最大辺の差0px、中心ズレ最大0.5px。


---

## 10. 追加機能：記録（フィードバック）と目安の手動調整

「体感温度が何℃のとき実際に何を着て、どう感じたか」を残し、それをもとに
服装の目安をずらせるようにした。**自動では変えない。適用はユーザーが毎回決める。**

### しきい値の持ち方

| | 変更前 | 変更後 |
|---|---|---|
| 保持 | 既定値＋オフセット（−2/0/＋2の3択） | **5つの境界値そのものを保持**（`fy.th`） |
| 編集 | プリセットのみ | **1段階ずつ数字を直接編集**＋プリセット |
| 検査 | なし | 上から順に小さくなっていなければ**警告を出す** |

### 記録の中身

`{ 日付, 体感温度, 実際に着た段階, 快適度, メモ }` を localStorage（`fy.log`）に保存。
同じ日付は上書き。快適度は5段階で、**暑すぎた＋2 〜 寒すぎた−2**。

### 提案の出し方

- 記録が**3件以上**たまったら提案を出す
- 快適度の平均を取り、**1目盛り＝約2℃**として換算（服装1段階の幅が約4℃のため）
- ずらし量 ＝ −(平均) × 2、0.5℃ 刻み
- 平均が＋（暑かった側）なら**しきい値を下げて薄着寄り**にする
- 段階ごとの内訳も表示し、特定の段階だけ手で直せるようにした

### 保存先の判断

記録は **localStorage**。Artifact 専用のデータベースを使えば端末をまたげるが、
**手元の `app/` フォルダで動かなくなる**ため採らなかった。同じコードが
ローカルのPWAでも公開版でも動くことを優先している。
消えたときのために**書き出し／読み込み（JSON）**を付けた。しきい値も一緒に入る。


---

## 11. 追加：気圧と、表示の拡大

### 気圧

| 項目 | 決定 |
|---|---|
| データ | `pressure_msl`（海面更正気圧）。16日分すべて時間別で取得可（408件中 欠損2件） |
| 見るもの | **絶対値と変化量の両方** |
| 置き場所 | **今日の欄に数字で。** 今日を大きく、あした・あさっては小さく |
| 期間 | **3日先まで** |
| 絶対値 | その日の**最低**気圧（いちばん低いところ） |
| 変化量 | **6時間でいちばん下がる幅**と、その時間帯（例「9時→15時 の 5.2hPa」） |
| 強調 | **6時間で −3hPa 以上**下がる日（設定で変更可）。豊橋16日分の実測で4日が該当 |
| 記録への体調追加 | **今回は入れない** |

**1hPa未満の上下は「下がる」と言わない。** 誤差の範囲で、毎日「0.1hPa下がります」と
出ると印として機能しなくなるため。

**色だけに頼らない。** 強調は色で行うが、下がり幅の数値（↓3.6）を必ず並べる。
色が見分けづらい環境でも情報が落ちないようにするため。

### 表示の拡大

| 項目 | 決定 |
|---|---|
| 対象 | 全体を一律に大きく＋**今日の欄を重点的に** |
| 14日リストの行 | **70px のまま**（文字だけ大きく）。1画面 約10.6日分は変えない |
| 行の情報 | **減らさない**（8要素のまま） |
| 文字サイズ | **設定で 標準 / 大きめ / もっと大きく**（1 / 1.12 / 1.25倍） |

実装上の注意が2つある。

1. **リストの列幅は px で固定する。** rem にすると文字を大きくしたとき日付欄まで広がり、
   本文の入る幅が足りず折り返して、行が 70px→139px と倍増した。px 固定で 104px に収まった
2. **CSS と JS には `?v=N` を付ける。** 付けないとブラウザが古いファイルを掴む。
   更新時は `index.html` の `?v=` と `sw.js` の `VERSION` を**両方**上げること
